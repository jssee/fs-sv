# Local env files must never ship in deployments. Vercel project env vars are
# the source of truth (set them in the dashboard or via `bun run env:production`);
# otherwise these localhost values would leak into the deployment.
.env
.env.*
**/.env
**/.env.*
!**/.env.example
